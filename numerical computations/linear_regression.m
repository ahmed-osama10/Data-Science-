clc
x=[2.5 3.5 5 6 7.5 10 12.5 15 17.5 20];
y=[5 3.4 2 1.6 1.2 0.8 0.6 0.4 0.3 0.3];
n=length(x);
Y=log(y);
sx=sum(x);
sx2=sum(x.^2);
sY=sum(Y);
sxY=sum(x.*Y);
coefMat=[n sx;sx sx2];
freeMat=[sY;sxY];
res=coefMat\freeMat;
B=res(2);
A=res(1);
a=exp(A);
Y_avg=sum(Y)/n;
st=sum((Y-Y_avg).^2);
sr=sum((Y-B-A*x).^2);
sY_x=sqrt(sr/(n-2));
r2=(st-sr)/st;
r=sqrt(r2);